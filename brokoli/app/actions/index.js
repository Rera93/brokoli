import * as ProjectActions from './projects';

/* 
   Grouping all the potential actions the app could perform
   in the ActionCreators const. Useful as app begins to grow.
*/
export const ActionCreators = Object.assign({},
    ProjectActions, 
);